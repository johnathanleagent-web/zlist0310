{
  "name": "zillow-scraper-proxy",
  "version": "1.0.0",
  "description": "Zillow listing proxy + Excel export server",
  "main": "server.js",
  "scripts": {
    "start": "pip install openpyxl Pillow --quiet --break-system-packages 2>/dev/null || pip install openpyxl Pillow --quiet; node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
